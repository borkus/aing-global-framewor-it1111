// DemoPage (AI Chat demo) removed – static build
import { AppLayout } from '@/components/layout/AppLayout';

export function DemoPage() {
  return (
    <AppLayout>
      <div className="flex items-center justify-center min-h-[60vh]">
        <p className="text-muted-foreground">Demo page unavailable in static build.</p>
      </div>
    </AppLayout>
  );
}
